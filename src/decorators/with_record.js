// import {withReducer} from 'recompose';
// export default (name, initialRecord) => withReducer(
//   name,
//   name + 'Update',
//   (state, newState) => (state && newState) ? state.merge(newState) : newState,
//   initialRecord
// );
